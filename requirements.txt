# Runtime
fastapi==0.115.6
uvicorn[standard]==0.32.1
asyncpg==0.30.0
pydantic==2.10.4
python-dotenv==1.0.1   # loads .env in local dev via startup script

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1          # ASGI test client used by FastAPI's TestClient
coverage[toml]==7.6.10
